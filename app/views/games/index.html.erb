<table class = "table table-hover">
  <tr>
    <th>Game</th>
    <th>Release Date</th>
    <th>Platforms</th>
  </tr>
  <% @games.each do |game| %>
    <tr>
      <td><%= game.name %></td>
      <td><%= game.id %></td>
      <td><%= game.name %></td>
    </tr>
  <% end %>
</table>