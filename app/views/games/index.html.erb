<table class="table table-hover">
  <tr>
    <th>Game</th>
  </tr>
  <% @games.each do |game| %>
    <tr>
      <td><a href="/games/<%= game.id %>"><%= game.name %></a></td>
    </tr>
  <% end %>
</table>