<table class="table table-hover">
  <tr>
    <th>User</th>
    <th>Game</th>
  </tr>
  <%= @user_games.each do |user_game| %>
    <tr>
      <td><%= User.find_by(user_game.user_id).first_name %></td>
      <td><%= Game.find_by(user_game.game_id).name %></td>
    </tr>
  <% end %>
</table>